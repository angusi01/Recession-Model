# Refresh Workflow

name: Refresh

on:
  schedule:
    - cron: '0 * * * *'

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - name: Refresh Data
        run: |
          curl -k https://api.example.com/refresh
